# EcoReport Failures, Fallbacks, and User Actions

이 문서는 EcoReport를 운영하면서 드러난 실패 지점, 현재 적용 중인 폴백, 그리고 아직 사용자 개입이 필요한 항목을 한 곳에 정리한 기록입니다.

목표는 세 가지입니다.

- 무엇이 아직 완전히 자동화되지 않았는지 빠르게 파악
- 왜 특정 값이나 화면이 기대와 다르게 보이는지 설명
- 다음 개선 우선순위를 명확하게 유지

## 1. 실제로 실패했거나 자주 흔들린 지점

### 1-1. 웹 GPT 자동 왕복은 운영 핵심으로 쓰기 어려웠음

문제:
- Safari/ChatGPT 웹 UI 자동화는 DOM 구조, 스트리밍 상태, 포커스 상태에 민감했습니다.
- 응답 완료 신호가 불안정해서, 답변이 끝나기 전에 읽거나 저장하는 일이 있었습니다.
- 파일 업로드/멀티모달 흐름을 완전 자동화하기 어렵습니다.

현재 결론:
- 웹 GPT 자동 전송은 보조 도구로만 유지
- 운영 핵심은 `수동 질문 파일 생성 -> 사용자가 직접 GPT/Gemini에 질의 -> 결과 저장`

현재 폴백:
- `knowledge/daily/manual-kit/YYYY-MM-DD/*` 질문 세트 생성
- `manual-llm` 저장 UI

### 1-2. Vercel 배포는 신뢰도 낮은 보조 채널로 내려감

문제:
- preview / production 배포 실패가 반복됨
- 로컬 파일 시스템 의존(`fs`)이 강해서 서버리스 배포와 궁합이 좋지 않았음
- 브리핑/analysis-state가 생성돼도 원격에서 바로 안 보이는 경우가 있었음

현재 결론:
- `localhost:3000` 이 운영 기준
- Vercel은 후순위 보조 채널

현재 폴백:
- Mac Mini 로컬 서버
- 같은 와이파이에서 LAN IP로 접속

### 1-3. 초기 리포트 수집이 1건만 남은 적이 있었음

문제:
- 테스트 실행에서 `--limit 1`로 만들어진 `index.json`이 재사용되면서, 실제로는 더 많은 리포트가 있었는데 1건만 존재하는 것처럼 보였습니다.

현재 결론:
- 수집 후에는 날짜별 `crawl-manifest`를 반드시 확인
- 필요하면 `--force`로 재수집

현재 폴백:
- `crawl-manifest.json`
- `crawl-manifest.md`

### 1-4. PDF 텍스트가 잘리거나 빈 경우가 있었음

문제:
- 초기에는 `extracted_text`가 길이 제한으로 잘렸음
- 일부 PDF는 스캔형이라 텍스트가 비어 있었음

현재 결론:
- 수집 후 전문 텍스트화를 필수 단계로 고정
- `pdftotext` 실패 시 OCR fallback

현재 폴백:
- `text/*.txt`
- `text-manifest.json`
- OCR fallback

### 1-5. 리포트 점수가 과도하게 넓게 잡히는 문제

문제:
- Stage 1 후보 영향만으로는 리포트가 너무 많은 계좌/종목에 연결되는 경향이 있었음
- 그 결과 `리포트 커버리지 100%` 같은 과대평가가 쉽게 나왔음

현재 결론:
- `impact-map.json` 확정 레이어를 도입
- 직접 종목/ETF 언급은 더 엄격하게 매핑

현재 상태:
- 1차 구현 완료
- 아직 더 좁히고 다듬어야 함

### 1-6. 계좌 코멘트가 사람다운 이유 설명으로 부족했음

문제:
- 단순 점수/비중/기술신호만 보여줘서 “왜 이 계좌를 이렇게 보나”가 약했음
- 매크로 사건과 계좌 액션의 연결이 느슨했음

현재 결론:
- `매크로 사건 -> 자산군 -> 계좌 액션` 흐름으로 코멘트 구성

현재 상태:
- 1차 강화 완료
- 더 자연스러운 문장 생성은 지속 개선 필요

## 2. 현재 운영에서 쓰는 폴백

### 2-1. Stage 2 LLM이 없을 때

폴백:
- `stage2-strategy-options.mock.json`

설명:
- Gemini/API가 없거나 실행하지 않을 때도 Stage 3/4를 검증 가능하게 유지

### 2-2. 기술 데이터가 비거나 일부 종목만 있을 때

폴백:
- coverage-aware weighting

설명:
- 기술/리포트/Stage 2 커버리지가 낮을수록 자동으로 배분 점수 비중이 커집니다.

### 2-3. 포트폴리오 스냅샷이 불완전할 때

폴백:
- `incomplete` 패널티 적용
- 대시보드에서 “부분 캡처” 경고 표시

설명:
- 누락 종목이 있으면 점수 자체보다 데이터 품질 이슈로 보고 감점합니다.

### 2-4. 원격 배포가 흔들릴 때

폴백:
- `http://localhost:3000`
- 같은 와이파이에서 `http://<MacMini-IP>:3000`

## 3. 아직 사용자 개입이 필요한 항목

### 3-1. 포트폴리오 캡처 업로드 / 보정

현재:
- 수동 업로드 + 저장 구조는 있음
- OCR 완전자동은 아직 미완성

사용자가 해야 할 일:
- 최신 계좌 캡처 업로드
- 숫자/종목 누락 여부 확인

### 3-2. Gemini Stage 2 실모델 실행

현재:
- 코드 있음
- 키가 없으면 mock 사용

사용자가 해야 할 일:
- `/Users/seo/Documents/Playground/EcoReport/.env` 에 `GEMINI_API_KEY` 입력

### 3-3. 웹 검색 기반 ETF 발굴

현재:
- 추천은 내부 리포트/테마/기술점수 기반
- 외부 웹 검색 기반 신규 ETF 발굴은 아직 없음

사용자가 해야 할 일:
- 아직 없음

남은 구현:
- 웹 검색 또는 ETF universe 확장

## 4. 현재 가장 부족한 것

### 4-1. 리포트 → 계좌 영향의 정밀도

아직 부족한 점:
- 어떤 리포트가 왜 ISA/PENSION/TOSS에 영향을 줬는지 더 정교하게 좁혀야 함
- category/account impact는 생겼지만, 강도/방향/근거 품질을 더 다듬어야 함

### 4-2. 모바일/PC 레이아웃 완전 분리

아직 부족한 점:
- 현재는 모바일/PC 의도 분리가 많이 들어갔지만, 완전한 2안 설계 수준은 아님

### 4-3. 리포트 표현 UI의 도메인 특화

아직 부족한 점:
- 지수/환율/원자재/종목/섹터를 더 강하게 시각 구분할 수 있음
- 예: KOSDAQ, USD/KRW, WTI 등 도메인별 전용 칩/배지 시스템

## 5. 운영 우선순위

1. `localhost:3000` 기준으로 항상 먼저 검증
2. 포트폴리오 스냅샷 품질 유지
3. Stage 2 실모델 연결
4. `impact-map` 정밀도 개선
5. 모바일/PC 전용 UI 완성
6. ETF 외부 탐색 추가

## 6. 빠른 체크리스트

매일 확인:
- 리포트 수집 수가 정상인가
- `text-manifest` 에 OCR/실패 건이 과도하지 않은가
- 포트폴리오 스냅샷이 최신인가
- Stage 2가 mock인지 실모델인지
- Stage 3에서 리포트 커버리지가 과도하게 높지 않은가
- Stage 4 코멘트가 실제 읽을 만한 문장인가
- `localhost:3000` 홈/리포트/포트폴리오가 모두 정상인가
